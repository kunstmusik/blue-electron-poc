/**
 * AutomatableLayer — a Layer that supports automation parameters.
 * Mirrors the Java AutomatableLayer interface.
 */
import { Layer } from './layer';

export interface AutomatableLayer extends Layer {
  // getAutomationParameters(): ParameterIdList; // Phase 9
}
