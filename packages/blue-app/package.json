{
  "name": "@blue/app",
  "version": "0.0.1",
  "description": "Electron application for Blue — loads .blue files and plays via blue-engine",
  "main": "dist/main/main.js",
  "scripts": {
    "build:main": "tsc -p tsconfig.main.json",
    "build:preload": "tsc -p tsconfig.preload.json",
    "build:renderer": "mkdir -p dist/renderer && cp src/renderer/index.html src/renderer/app.js src/renderer/styles.css dist/renderer/",
    "build": "pnpm build:main && pnpm build:preload && pnpm build:renderer",
    "start": "electron .",
    "test": "echo \"TODO: tests\" && exit 0"
  },
  "dependencies": {
    "@blue/data": "workspace:*"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "@electron/remote": "^2.1.0",
    "typescript": "^5.8.0"
  }
}
